#include "main.h"

/**
 * * _isupper - evalue if letter is in uppercase.
 * @c: print int
 * Return: returns zero
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

else
{
return (0);
}
}
